const config = {
  // 启动端口
  port: 3000,
  secret: 'huangjingchao0127',

  // 数据库配置
  database: {
    DATABASE: 'koa',
    USERNAME: 'root',
    PASSWORD: '123456',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config